rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all documents to fix synchronization block
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
